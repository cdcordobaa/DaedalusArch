# U0 Code Generation Plan — Shared Extensions

## Unit Context
- **Unit**: U0 — Shared Extensions (Foundation)
- **Track**: Main (pre-worktree)
- **Stories**: S-INIT-04 (exclude paths), S-INIT-05 (enable/disable), S-INIT-06 (validation, partial)
- **Dependencies**: None — first unit
- **Constraint**: All 312 existing tests must pass after changes

## Files to Modify (Brownfield)
- `src/shared/types/spec.ts` — extend FitnessFunction type
- `src/spec-parser/layer-parsers.ts` — parse enabled + exclude_paths
- `src/spec-parser/spec-schema.ts` — update JSON schema (if used)
- `src/fitness-compiler/fitness-compiler.ts` — add filterEnabled + exclude injection
- `src/shared/index.ts` — export new types

## Files to Create (New)
- `src/shared/types/validation.ts` — ValidationReport types
- `src/shared/types/baseline.ts` — BaselineSnapshot/Result types  
- `src/shared/types/llm-config.ts` — GeminiConfig, LLMProviderConfig
- `src/spec-parser/spec-validator.ts` — validation logic
- `src/fitness-compiler/glob-to-regex.ts` — glob conversion utility
- `src/fitness-compiler/exclude-injector.ts` — Cypher WHERE NOT injection

## Test Files to Create/Modify
- `src/spec-parser/__tests__/layer-parsers.test.ts` — new tests for enabled/exclude parsing
- `src/spec-parser/__tests__/spec-validator.test.ts` — new validation tests
- `src/fitness-compiler/__tests__/glob-to-regex.test.ts` — glob conversion tests
- `src/fitness-compiler/__tests__/exclude-injector.test.ts` — WHERE NOT injection tests
- `src/fitness-compiler/__tests__/fitness-compiler.test.ts` — extend with enabled/disabled tests

---

## Execution Steps

### Step 1: Extend shared domain types
- [x] Add `enabled`, `excludePaths`, `disabledReason` to FitnessFunction in `src/shared/types/spec.ts`
- [x] Add `DisabledFunction` interface to `src/shared/types/spec.ts`
- [x] Create `src/shared/types/validation.ts` with ValidationReport, ValidationError, ValidationSummary, ValidationErrorCode
- [x] Create `src/shared/types/baseline.ts` with BaselineSnapshot, BaselineEntry, BaselineResult, BaselineStatus
- [x] Create `src/shared/types/llm-config.ts` with GeminiConfig, LLMProviderConfig
- [x] Add `ActionableViolation` extending Violation in existing violation types
- [x] Update `src/shared/index.ts` to export all new types
- **Stories**: S-INIT-04, S-INIT-05, S-INIT-06

### Step 2: Verify existing tests still pass
- [x] Run full test suite (`npm test`) — all 312 tests must pass
- [x] Fix any type errors from Step 1 (default values for new optional fields)
- **Gate**: Do not proceed if any test fails

### Step 3: Extend spec parser — enabled + exclude_paths parsing
- [x] Modify `parseLayerB()` in `src/spec-parser/layer-parsers.ts` to parse `enabled`, `exclude_paths`, `reason` fields
- [x] Default `enabled` to `true`, `excludePaths` to `[]`
- [x] Update template merge logic to handle enabled/excludePaths overrides
- [x] Update spec schema validation if JSON Schema is used (`src/spec-parser/spec-schema.ts`)
- **Stories**: S-INIT-04, S-INIT-05

### Step 4: Tests for spec parser extensions
- [x] Add tests for parsing `enabled: false` with reason
- [x] Add tests for parsing `exclude_paths` array
- [x] Add tests for default values (missing fields)
- [x] Add tests for template merge with enabled/excludePaths overrides
- [x] Add backward compatibility test (v1.0 spec parses unchanged)
- **Stories**: S-INIT-04, S-INIT-05

### Step 5: Implement glob-to-regex utility
- [x] Create `src/fitness-compiler/glob-to-regex.ts`
- [x] Implement `globToRegex(pattern: string): string`
- [x] Handle: `*`, `**`, `?`, literal dots, anchoring
- [x] Throw on invalid patterns
- **Stories**: S-INIT-04

### Step 6: Tests for glob-to-regex
- [x] Test `*` → single level match
- [x] Test `**` → recursive match
- [x] Test `?` → single character
- [x] Test literal paths
- [x] Test complex patterns: `src/**/*.spec.ts`, `src/cli/**`
- [x] Test dot escaping: `*.ts` doesn't match `xts`
- [x] Test anchoring: pattern matches full path, not substring
- [x] Test invalid pattern handling
- **Stories**: S-INIT-04

### Step 7: Implement exclude path Cypher injector
- [x] Create `src/fitness-compiler/exclude-injector.ts`
- [x] Implement `injectExcludePaths(cypher: string, excludePaths: string[]): { cypher: string, params: Record<string, unknown> }`
- [x] Detect node alias from RETURN clause (source vs n vs file etc.)
- [x] Append `AND NONE(pattern IN $excludePatterns WHERE <alias>.filePath =~ pattern)` before RETURN
- [x] Pass converted regex patterns as Neo4j parameters
- [x] Return original cypher unchanged if excludePaths is empty
- **Stories**: S-INIT-04

### Step 8: Tests for exclude injector
- [x] Test injection into a simple MATCH...WHERE...RETURN query
- [x] Test with different node aliases (source, n, file)
- [x] Test empty excludePaths returns unchanged cypher
- [x] Test multiple exclude patterns
- [x] Test params contain converted regex patterns
- **Stories**: S-INIT-04

### Step 9: Extend fitness compiler — filterEnabled + exclude injection
- [x] Add `filterEnabled()` to `src/fitness-compiler/fitness-compiler.ts`
- [x] Call `filterEnabled()` at the top of `compileFunctions()` before compilation
- [x] After `compileSymbolic()`, call `injectExcludePaths()` if function has excludePaths
- [x] Include `disabledFunctions` in `CompiledFunctions` output
- [x] Update `CompiledFunctions` type in compiler types
- **Stories**: S-INIT-04, S-INIT-05

### Step 10: Tests for compiler extensions
- [x] Test that disabled functions are excluded from compilation
- [x] Test that disabled functions appear in `disabledFunctions` output
- [x] Test that exclude_paths are injected into compiled Cypher
- [x] Test backward compat: functions without enabled/excludePaths compile normally
- [x] Run full test suite — all 312 + new tests pass
- **Stories**: S-INIT-04, S-INIT-05

### Step 11: Implement spec validator
- [x] Create `src/spec-parser/spec-validator.ts`
- [x] Implement `validateSpec(spec: ParsedSpec, projectPath: string): DomainResult<ValidationReport>`
- [x] Implement all 8 validation checks from business rules
- [x] Import `globToRegex` from fitness-compiler for glob validation
- **Stories**: S-INIT-06

### Step 12: Tests for spec validator
- [x] Test layer dir not found detection
- [x] Test duplicate function ID detection
- [x] Test invalid template reference detection
- [x] Test invalid threshold detection
- [x] Test invalid glob pattern detection
- [x] Test valid spec returns no errors
- [x] Test summary counts are correct
- **Stories**: S-INIT-06

### Step 13: Final verification
- [x] Run full test suite: `npm test`
- [x] Run type check: `npx tsc --noEmit`
- [x] Verify all 312 original tests pass (backward compat)
- [x] Count new tests added
- [x] Update `src/shared/index.ts` if any exports missing

---

## Summary
- **Modified files**: ~6 (shared types, spec-parser, fitness-compiler)
- **New files**: ~6 source + ~5 test files
- **Estimated new tests**: ~40-50
- **Stories covered**: S-INIT-04 (full), S-INIT-05 (full), S-INIT-06 (validation logic, CLI command in U1)
