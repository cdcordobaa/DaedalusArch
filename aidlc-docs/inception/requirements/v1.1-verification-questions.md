# v1.1 Requirements Verification — Demo-Focused Re-Scoping

The original v1.1 requirements defined 14 FRs. Based on our discussion, we're narrowing to a **demoable product** with two parallel tracks. These questions confirm the scope and resolve ambiguities before proceeding.

---

## Question 1
**Scope confirmation**: The proposed demo-focused v1.1 includes these FRs. Confirm or adjust:

**Track A (Spec Generation)**: FR-01 (Agent Skill), FR-02 (firewall init), FR-03 (Exclude Paths), FR-06 (Framework Presets), FR-14 (Spec Validation)
**Track B (Demo Dashboard)**: FR-07 (Actionable Violations) + NEW (HTML report generator)
**Deferred**: FR-04, FR-05, FR-08, FR-09, FR-10, FR-11, FR-12

A) Confirmed — build exactly the FRs listed above
B) Add FR-04 (Rule Enable/Disable) — it's cheap and useful for the demo
C) Add FR-09 (Incremental Rule Adoption) — baseline mechanism is key to the brownfield story
D) Add both FR-04 and FR-09
E) Other (please describe after [Answer]: tag below)

[Answer]: D

---

## Question 2
**FR-13 (Real LLM Providers)**: The skill in FR-01 needs a real LLM client (not the mock). What provider scope for the demo?

A) Claude only (Anthropic SDK) ��� single provider, fastest to ship
B) Claude + OpenAI — two providers, more flexibility for demo audience
C) Provider-agnostic interface with Claude implementation only — future-proof but minimal
D) Other (please describe after [Answer]: tag below)

[Answer]: D — GeminiProvider (Google AI SDK) for the neuronal evaluation path. Init/spec generation handled by Claude Code skill natively (no coded LLM call needed). FR-13 scoped to Gemini only.

---

## Question 3
**FR-01 (Agent Skill) implementation surface**: The original FR-01 listed three surfaces: MCP tool, Cursor tool, CLI command. For the demo, which surfaces do we actually build?

A) Claude Code skill only (`.claude/commands/`) — fastest, demo uses Claude Code
B) CLI command (`firewall init`) only — works everywhere, not agent-dependent
C) Both Claude Code skill AND CLI command — skill calls CLI under the hood
D) MCP tool — works across Claude Code, Cursor, and any MCP-compatible agent
E) Other (please describe after [Answer]: tag below)

[Answer]: A

---

## Question 4
**FR-06 (Framework Presets)**: How many presets do we ship for the demo?

A) Clean architecture only — one preset, prove the concept
B) Clean architecture + NestJS — two presets, shows framework awareness
C) Clean architecture + NestJS + Express — three presets, covers most TS backends
D) All 6 from the original FR-06 (plain-ts, nestjs, nextjs, express, react-vite, monorepo)
E) Other (please describe after [Answer]: tag below)

[Answer]: B

---

## Question 5
**Demo Dashboard — data source**: How does the HTML dashboard get its data?

A) Embedded JSON — `firewall report` bakes the evaluation JSON directly into the HTML file (fully self-contained, zero server)
B) Companion JSON file — HTML loads `results.json` from the same directory via fetch (needs a local server or file:// workaround)
C) Both modes — embedded by default, optional `--external-data` flag for companion file
D) Other (please describe after [Answer]: tag below)

[Answer]: A

---

## Question 6
**Demo Dashboard — graph visualization**: The architecture map is the visual centerpiece. What rendering approach?

A) D3.js force-directed graph — maximum control, classic choice, larger learning curve
B) vis.js Network — simpler API, good defaults for node-edge graphs, lighter weight
C) Mermaid.js embedded — consistent with aidlc-docs diagrams, limited interactivity
D) Cytoscape.js — purpose-built for graph analysis, supports layouts like dagre/hierarchical
E) Other (please describe after [Answer]: tag below)

[Answer]: D

---

## Question 7
**Demo target repo**: You're searching for an open-source TypeScript repo to demo against. Should the v1.1 implementation be tested against a specific repo during development, or just against the existing v1.0 test fixtures?

A) Develop against existing v1.0 fixtures only — find demo repo later
B) Pick one open-source repo early, develop and test against it throughout — the demo repo IS the test target
C) Both — v1.0 fixtures for unit tests, open-source repo for integration/demo testing
D) Other (please describe after [Answer]: tag below)

[Answer]: C

---

## Question 8
**Construction parallelism (Option C)**: At code generation time, we'll split into worktrees. Should both tracks share a single Build & Test phase at the end, or should each track have its own?

A) Shared Build & Test — merge both tracks into main first, then one Build & Test pass covering everything
B) Separate Build & Test per track — each track validates independently, then a final integration test after merge
C) Other (please describe after [Answer]: tag below)

[Answer]: A

---

## Question 9
**In-memory graph (FR-12)**: Currently deferred, but it directly impacts the demo story. Without it, the demo requires Docker + Neo4j running. Is that acceptable for the demo?

A) Acceptable — demo audience understands infrastructure prerequisites, Neo4j is fine
B) Not acceptable — pull FR-12 back into scope, the "zero infrastructure" promise matters for the demo
C) Compromise — use SQLite or a simple adjacency list as a lightweight alternative (not full FR-12)
D) Other (please describe after [Answer]: tag below)

[Answer]: A

---

## Question 10
**Naming**: The Notion doc uses "archguard" as the product name. The current codebase uses "DaedalusArch" / "firewall". What name should the demo use?

A) `archguard` — the product name from the Notion doc (archguard init, archguard check, archguard report)
B) `firewall` — keep the current CLI name (firewall init, firewall evaluate, firewall report)
C) `daedalus` — use the project codename
D) Decide later — keep `firewall` for now, rename is a separate task
E) Other (please describe after [Answer]: tag below)

[Answer]: E — "architectonic firewall" (CLI name TBD — likely `archwall` or `arcfw` or keep `firewall`)
