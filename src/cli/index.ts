/**
 * CLI — C9
 * Bounded Context: command-line interface, user interaction, output formatting.
 * Commands: evaluate | batch | drift
 * Flags: --project, --spec, --dir, --output, --format, --verbose, --neo4j-uri, --symbolic-only, --neuronal-only
 *
 * Implementation: Unit 7 (U7 — CLI + CI/CD)
 */
export type {};
